# AOS-Browser 

(本地LLM专用版) 架构确认与顶层设计（决策落地版） > **约束前提**：仅使用本地部署LLM（llama.cpp定制版），无云端依赖，完全离线可用 --- ## 一、关键决策确认汇总 | 决策项 | 确认方案 | 实现要点 | 配置项 | |--------|----------|----------|--------| | **KV Cache摘要** | 定制llama.cpp实现Export/Import接口 | 轻量摘要<100MB，非完整显存；恢复时加速推理 | `llm.enable_kv_hint: true/false` | | **critical_dom_hash** | 可配置选择器路径哈希算法 | 基于`config.dom_critical_selectors`列表计算DOM指纹 | `dom_critical_selectors: ["#main-form", "input[name=email]"]` | | **安全加密** | 方案B：关键字段正则匹配加密 | 仅加密密码/Token等敏感字段，通过正则配置匹配 | `security.sensitive_field_patterns: ["password", "token", "auth"]` | | **可观测性** | 方案A：仅本地日志，完全离线 | 核心指标输出到本地文件，预留插件接口但不实现远程上报 | `metrics.output_path: "/var/log/aos-browser/metrics.log"` | --- ## 二、用户需求规格（UR - 更新版） | 编号 | 需求类别 | 需求描述 | 验收标准 | |------|----------|----------|----------| | **UR-01** | 运行时环境 | Linux用户态运行，标准内核，C++20协程支持 | 无需PREEMPT_RT补丁，g++-12+编译通过 | | **UR-02** | 本地LLM约束 | 支持7B-14B参数量化模型（4/5-bit），单实例部署 | 模型加载<30s，推理延迟50-200ms/token（RTX 3060+） | | **UR-03** | 中断响应 | 用户暂停指令响应<100ms；浏览器崩溃检测<500ms | 实测P99延迟达标，无感知卡顿 | | **UR-04** | 状态持久化 | 浏览器状态（URL/Cookie/Storage/Forms/critical_dom_hash）崩溃后100%可恢复 | 模拟崩溃测试，恢复后页面状态与崩溃前一致 | | **UR-05** | LLM可抢占 | 推理过程中支持协作式取消，每Token检查取消信号；支持KV摘要断点续推 | 用户暂停时，推理在≤2个Token内停止；恢复时若启用KV提示，推理加速≥30% | | **UR-06** | 资源约束 | 单实例内存<16GB（含模型+浏览器+系统）；支持≥3并发标签页 | 长时间运行（24h）内存无泄漏，OOM概率<0.1% | | **UR-07** | 安全审计 | 敏感字段（正则匹配）加密存储；关键操作记录本地审计日志 | 加密字段无法明文读取；审计日志可追溯操作序列 | | **UR-08** | 可观测性 | 核心指标采集输出到本地日志文件；支持调试模式详细输出 | 默认性能开销<1%，日志文件可轮转，不依赖外部服务 | | **UR-09** | 离线可用 | 无外部网络依赖（除目标网页），模型/配置/日志全部本地 | 断网环境下可正常启动、执行、恢复任务 | | **UR-10** | 扩展兼容 | 架构预留v4.0进化接口（Skill编译/轨迹学习），通过编译期配置控制 | 核心模块接口稳定，扩展点默认禁用，避免二进制膨胀 | --- ## 三、架构决策记录（ADR - 最终版） ### ADR-01 ~ ADR-10：保持不变（见上一版） ### ADR-11: KV Cache摘要通过定制llama.cpp实现 | 属性 | 内容 | |------|------| | **决策** | 通过定制llama.cpp分支，实现`ExportKVCacheSummary()`/`ImportKVCacheSummary()`接口，导出轻量级注意力模式摘要（<100MB） | | **理由** | 完整KV Cache保存开销大（7B模型4-6GB），但完全重推成本高（<30s）；轻量摘要可在恢复时加速推理30%+，性价比最优 | | **影响** | ✅ 崩溃恢复体验显著提升 ❌ 需维护llama.cpp定制分支，增加构建复杂度 | | **实现要点** | ```cpp // 定制llama.cpp接口（外部声明） extern "C" { // 导出轻量摘要：仅保存关键层的注意力模式+位置编码统计 llama_kv_summary_t llama_export_kv_summary(llama_context* ctx, uint32_t max_layers = 8); // 导入摘要：加速后续推理的预填充阶段 void llama_import_kv_summary(llama_context* ctx, llama_kv_summary_t summary); // 释放摘要资源 void llama_free_kv_summary(llama_kv_summary_t summary); } // LocalLLMAdapter封装 class LocalLLMAdapter { public: std::optional<KVCacheSummary> ExportKVCacheHint() { if (!config_.enable_kv_hint) return std::nullopt; auto summary = llama_export_kv_summary(ctx_, config_.kv_hint_max_layers); return KVCacheSummary{ .data = Serialize(summary), // 序列化为二进制 .token_count = llama_get_kv_cache_token_count(ctx_), .checksum = ComputeChecksum(summary) }; } void ImportKVCacheHint(const KVCacheSummary& summary) { if (!config_.enable_kv_hint) return; auto deserialized = Deserialize(summary.data); if (ComputeChecksum(deserialized) != summary.checksum) { LOG_WARN("KV summary checksum mismatch, skipping import"); return; } llama_import_kv_summary(ctx_, deserialized); } }; ``` | **配置项** | `llm.enable_kv_hint: true`（默认false，V1.1启用） | ### ADR-12: critical_dom_hash采用可配置选择器路径哈希 | 属性 | 内容 | |------|------| | **决策** | 基于`config.dom_critical_selectors`配置列表，提取关键选择器路径计算SHA-256哈希，作为页面状态恢复校验指纹 | | **理由** | 完整DOM哈希开销大且易受动态内容干扰；关键选择器路径（如表单、主内容区）更能反映任务相关状态 | | **影响** | ✅ 恢复校验精准，计算开销低（<1ms） ❌ 需合理配置选择器列表，避免漏判/误判 | | **实现要点** | ```cpp class CriticalDOMHasher { public: // 初始化：加载配置的选择器列表 void Init(const std::vector<std::string>& critical_selectors) { selectors_ = critical_selectors; // 预编译选择器（Playwright支持） for (const auto& sel : selectors_) { compiled_.push_back(CompileSelector(sel)); } } // 计算当前页面critical_dom_hash std::string ComputeHash(const BrowserContext& ctx) { std::string concatenated; for (const auto& compiled : compiled_) { // 提取元素的关键属性：tag+id+class+name（忽略动态值） auto fingerprint = ExtractElementFingerprint(ctx, compiled); if (!fingerprint.empty()) { concatenated += fingerprint + "|"; } } return SHA256(concatenated); // 返回64字符hex } private: std::string ExtractElementFingerprint(const BrowserContext& ctx, const CompiledSelector& sel) { auto element = ctx.FindElement(sel); if (!element) return ""; // 关键：只提取稳定属性，忽略动态内容 return fmt::format("{}#{}.{},name={}", element.tag_name, element.id.empty() ? "" : element.id, element.class_names.empty() ? "" : element.class_names.front(), element.name.empty() ? "" : element.name // 表单字段名 ); } std::vector<std::string> selectors_; std::vector<CompiledSelector> compiled_; }; ``` | **配置示例** | ```yaml dom: critical_selectors: - "#main-form" - "input[name=email]" - "input[name=password]" - "#submit-button" - ".product-price" # 比价场景关键 - ".cart-total" # 购物场景关键 ``` ### ADR-13: 安全加密采用关键字段正则匹配（方案B） | 属性 | 内容 | |------|------| | **决策** | 仅对匹配`security.sensitive_field_patterns`正则的字段进行AES-GCM加密，其他字段明文存储 | | **理由** | 全字段加密开销大且调试困难；关键字段（密码/Token/认证信息）加密已满足安全合规要求 | | **影响** | ✅ 性能开销<5%，调试友好 ❌ 需严格审计正则配置，避免敏感字段漏配 | | **实现要点** | ```cpp class SecurityManager { public: void Init(const SecurityConfig& config) { config_ = config; // 预编译正则表达式 for (const auto& pattern : config.sensitive_field_patterns) { regexes_.push_back(std::regex(pattern, std::regex::icase)); } // 加载加密密钥（外部注入） encryption_key_ = LoadKeyFromFile(config.encryption_key_path); } // 加密敏感字段（序列化前调用） nlohmann::json EncryptSensitiveFields(const nlohmann::json& data) { auto result = data; EncryptRecursive(result); return result; } // 解密敏感字段（反序列化后调用） nlohmann::json DecryptSensitiveFields(const nlohmann::json& data) { auto result = data; DecryptRecursive(result); return result; } private: void EncryptRecursive(nlohmann::json& node) { if (node.is_object()) { for (auto& [key, value] : node.items()) { // 检查字段名是否匹配敏感模式 if (IsSensitiveFieldName(key)) { if (value.is_string()) { value = { {"__encrypted__", true}, {"data", AES_GCM_Encrypt(value.get<std::string>(), encryption_key_)} }; } } else { EncryptRecursive(value); // 递归处理嵌套对象 } } } else if (node.is_array()) { for (auto& item : node) { EncryptRecursive(item); } } } bool IsSensitiveFieldName(const std::string& field_name) { for (const auto& regex : regexes_) { if (std::regex_match(field_name, regex)) { return true; } } return false; } SecurityConfig config_; std::vector<std::regex> regexes_; EncryptionKey encryption_key_; }; ``` | **配置示例** | ```yaml security: encrypt_sensitive_fields: true encryption_key_path: "/etc/aos-browser/key.bin" # 外部注入，权限600 sensitive_field_patterns: - ".*password.*" - ".*token.*" - ".*auth.*" - ".*secret.*" - ".*api_key.*" audit_critical_ops: true audit_log_path: "/var/log/aos-browser/audit.log" ``` ### ADR-14: 可观测性仅本地日志输出（方案A） | 属性 | 内容 | |------|------| | **决策** | 核心指标输出到本地日志文件，支持轮转；预留`MetricsSink`插件接口，但默认不实现远程上报 | | **理由** | 完全离线场景无需依赖外部监控服务；本地日志满足调试与基础审计需求，避免网络依赖 | | **影响** | ✅ 完全离线可用，部署简单 ❌ 高级监控（如Prometheus）需二次开发插件 | | **实现要点** | ```cpp class LocalMetricsCollector { public: void Init(const MetricsConfig& config) { config_ = config; // 初始化日志轮转：每100MB或每天轮转 log_rotator_ = std::make_unique<LogRotator>( config.output_path, config.max_file_size_mb, config.max_backup_files ); } // 记录核心指标（线程安全） void Record(const MetricsSnapshot& metrics) { std::lock_guard lock(mutex_); // 格式化输出：时间戳 + 指标KV auto line = fmt::format("[{}] {}\n", std::chrono::system_clock::now().time_since_epoch().count(), SerializeMetrics(metrics) // JSON格式，便于后续解析 ); log_rotator_->Write(line); // 调试模式：同时输出到stderr if (config_.debug_mode) { std::cerr << line; } } // 预留插件接口（默认空实现） void RegisterSink(std::unique_ptr<MetricsSink> sink) { // 方案A：不启用远程上报，直接丢弃 // 方案B扩展：可注册Prometheus/HTTP端点插件 } private: std::string SerializeMetrics(const MetricsSnapshot& m) { return fmt::format( "interrupt_count={} recovery_success={} pause_latency_ms={:.2f} steps_completed={}", m.interrupt_count.load(), m.recovery_success.load(), m.avg_pause_latency_ms.load(), m.steps_completed.load() ); } MetricsConfig config_; std::unique_ptr<LogRotator> log_rotator_; std::mutex mutex_; }; ``` | **配置示例** | ```yaml metrics: output_path: "/var/log/aos-browser/metrics.log" max_file_size_mb: 100 max_backup_files: 5 debug_mode: false # 生产环境关闭，避免stderr刷屏 # remote_endpoint: "" # 方案B扩展项，默认留空 ``` --- ## 四、顶层架构设计（决策落地版） ``` ┌─────────────────────────────────────────────────────────────────────┐ │ Layer 3: Meta-Cognition (元认知层) │ │ ├─ GoalDriftDetector │ URL/DOM变化 + LLM周期性评估 │ │ ├─ AdaptiveContextCompactor│ 分层压缩：关键保留 + 分批摘要 + 递归兜底 │ │ ├─ MinimalTabCoordinator │ 多标签协调：优先级 + 冲突检测 + 资源限制 │ │ └─ LocalMetricsCollector │ 本地日志输出 + 轮转 + 插件接口预留 │ └───────────────────────────┬─────────────────────────────────────────┘ │ 策略注入 / 指标记录 ┌───────────────────────────▼─────────────────────────────────────────┐ │ Layer 2: Cognitive Coroutine (认知协程层) │ │ ├─ PreemptibleReActEngine │ │ │ ├─ PlanningStep │ 可挂起/恢复，每关键操作检查ShouldPause() │ │ │ ├─ ActingStep │ 浏览器操作原子化，导航前自动快照 │ │ │ └─ ObservingStep │ DOM/截图/网络状态并行采集（std::when_all） │ │ ├─ LocalLLMAdapter (定制llama.cpp封装) │ │ │ ├─ GenerateWithCancel(prompt, cancel_token) │ 每Token检查取消 │ │ │ ├─ ExportKVCacheSummary() / ImportKVCacheSummary() │ 定制接口 │ │ │ └─ HotLoad/Unload │ 内存紧张时释放模型，支持后续热加载 │ │ ├─ BrowserSnapshotManager (增强版) │ │ │ ├─ Capture: url/cookies/storage/forms/viewport/critical_dom_hash│ │ │ ├─ SaveAtomic: tmp+fsync+atomic_rename │ │ │ └─ VerifyRestoration: critical_dom_hash校验防止脏恢复 │ │ ├─ PersistentContext (SQLite WAL模式) │ │ │ ├─ tasks: 任务主表（状态/目标/浏览器快照JSON） │ │ │ ├─ react_steps: 历史步骤表（支持分片加载） │ │ │ ├─ checkpoints: 原子检查点表（tmp_path/final_path/checksum） │ │ │ └─ audit_log: 审计日志表（操作/时间/结果/加密标记） │ │ └─ SecurityManager │ │ ├─ EncryptSensitiveFields: 正则匹配 + AES-GCM加密 │ │ ├─ AuditCriticalOps: 记录FORM_SUBMIT/LOGIN_ATTEMPT等事件 │ │ └─ DomainWhitelist: 限制Agent可访问的域名（可选） │ └───────────────────────────┬─────────────────────────────────────────┘ │ 事件驱动 / 协程调度 ┌───────────────────────────▼─────────────────────────────────────────┐ │ Layer 1: Event & Control (事件与控制层) │ │ ├─ LightweightInterruptQueue │ │ │ ├─ PostUserPause() / ShouldPause() │ <100ns检查，原子信号 │ │ │ ├─ PriorityCompare: USER > SYSTEM > BACKGROUND │ │ │ └─ Event: {type, priority, payload, timestamp} │ │ ├─ CDPEventListener │ │ │ ├─ 监听CDP事件：navigation_complete / dialog_opening / crashed │ │ │ └─ 协议解析：CDP JSON → 结构化BrowserEvent │ │ ├─ InterruptController │ │ │ ├─ 路由用户暂停/崩溃/弹窗信号到对应处理器 │ │ │ └─ 紧急制动：触发BrowserSnapshot紧急保存 │ │ └─ RecoveryManager │ │ ├─ 读取原子检查点 → 校验checksum → 重建浏览器实例 │ │ ├─ 恢复Cookie/Storage/URL → 重建协程上下文 │ │ ├─ 校验critical_dom_hash匹配（防止脏恢复） │ │ └─ 从Prompt历史重推LLM（或加载KV摘要加速） │ └───────────────────────────┬─────────────────────────────────────────┘ │ 系统调用 / 资源管理 ┌───────────────────────────▼─────────────────────────────────────────┐ │ Layer 0: Runtime & Storage (运行时与存储) │ │ ├─ PlaywrightAdapter │ │ │ ├─ 浏览器生命周期管理：Launch/Navigate/Close │ │ │ ├─ 操作原语：Click/Type/Wait/ExtractDOM/Screenshot │ │ │ └─ 事件回调注册：CDP WebSocket → C++事件队列 │ │ ├─ LocalLLMRuntime (定制llama.cpp) │ │ │ ├─ ModelPool: 单例管理模型，支持热加载/卸载 │ │ │ ├─ InferenceStream: 流式生成 + 取消检查点 + 流式输出回调 │ │ │ ├─ KVCacheManager: 定制接口导出/导入轻量摘要（<100MB） │ │ │ └─ BuildConfig: 启用KV_HINT编译开关 │ │ ├─ SQLiteStorage │ │ │ ├─ WAL模式：支持并发读写，崩溃安全 │ │ │ ├─ 异步写入：检查点写入后台线程，避免阻塞主协程 │ │ │ └─ 连接池：单实例单连接，避免锁竞争 │ │ ├─ EncryptionService │ │ │ ├─ AES-GCM加密：敏感字段加密存储 │ │ │ ├─ 密钥管理：从文件/环境变量加载，内存中明文仅限必要时刻 │ │ │ └─ 正则匹配：基于配置的字段名识别敏感数据 │ │ ├─ CriticalDOMHasher │ │ │ ├─ 加载config.dom_critical_selectors配置 │ │ │ ├─ 预编译选择器，加速指纹计算 │ │ │ └─ 计算SHA-256哈希，用于恢复校验 │ │ └─ ConfigLoader │ │ ├─ 加载config.yaml + 环境变量覆盖 │ │ ├─ 校验必填项：model_path/encryption_key_path/dom_selectors等 │ │ └─ 热重载：部分配置（如日志级别）支持运行时更新 │ └─────────────────────────────────────────────────────────────────────┘ ``` --- ## 五、关键数据流更新（决策落地） ### 5.1 BrowserSnapshot 增强结构 ```cpp struct EnhancedBrowserSnapshot { // 基础状态 std::string url; std::vector<Cookie> cookies; std::string local_storage_json; std::vector<FilledForm> forms; // 【新增】页面渲染状态 ViewportState viewport; std::vector<FrameState> iframes; // 【新增】恢复校验（关键决策） std::string critical_dom_hash; // SHA-256，基于配置选择器计算 // 【新增】恢复校验元数据 uint64_t snapshot_checksum; // 防止脏恢复 std::chrono::steady_clock::time_point captured_at; // 序列化时：敏感字段经SecurityManager加密 nlohmann::json ToJson(const SecurityManager& sec_mgr) const { auto json = nlohmann::json{ {"url", url}, {"cookies", cookies}, {"local_storage", local_storage_json}, {"forms", forms}, {"viewport", viewport}, {"critical_dom_hash", critical_dom_hash}, {"checksum", snapshot_checksum}, {"captured_at", captured_at.time_since_epoch().count()} }; return sec_mgr.EncryptSensitiveFields(json); // 决策：仅加密敏感字段 } static EnhancedBrowserSnapshot FromJson( const nlohmann::json& j, const SecurityManager& sec_mgr ) { auto decrypted = sec_mgr.DecryptSensitiveFields(j); // 先解密 return { .url = decrypted.at("url").get<std::string>(), .cookies = decrypted.at("cookies").get<std::vector<Cookie>>(), // ... 其他字段 .critical_dom_hash = decrypted.at("critical_dom_hash").get<std::string>(), }; } }; ``` ### 5.2 崩溃恢复流程（集成critical_dom_hash校验） ```cpp Task<void> RecoveryManager::RecoverFromCrash(const std::string& task_id) { // 1. 读取原子检查点 auto checkpoint = co_await checkpoint_mgr_.Load(task_id); if (!checkpoint.has_value()) { LOG_ERROR("No valid checkpoint for task {}", task_id); co_return; } // 2. 启动新浏览器实例 auto browser = co_await playwright_.Launch(); // 3. 恢复浏览器状态 co_await browser_.RestoreCookies(checkpoint->snapshot.cookies); co_await browser_.SetLocalStorage(checkpoint->snapshot.local_storage_json); co_await browser_.Navigate(checkpoint->snapshot.url); // 4. 【关键】校验critical_dom_hash（决策落地） auto current_hash = critical_dom_hasher_.ComputeHash(browser_.GetContext()); if (current_hash != checkpoint->snapshot.critical_dom_hash) { LOG_WARN("DOM hash mismatch: expected={}, actual={}", checkpoint->snapshot.critical_dom_hash, current_hash); // 策略：记录告警但继续恢复（避免过度严格导致任务失败） metrics_.Record({.goal_drift_detected = 1}); } // 5. 重建协程，从Prompt历史重推（或加载KV摘要） if (config_.llm.enable_kv_hint && checkpoint->llm_kv_hint.has_value()) { LOG_INFO("Restoring with KV cache hint for faster inference"); llm_adapter_.ImportKVCacheHint(checkpoint->llm_kv_hint.value()); } co_await agent_.ResumeFromHistory(checkpoint->react_history); } ``` ### 5.3 本地LLM推理流程（集成KV摘要） ```cpp Task<LLMResult> LocalLLMAdapter::GenerateWithCancel( const std::string& prompt, CancelToken& cancel_token ) { // 创建流式推理上下文 auto stream = llama_cpp_.CreateStream(prompt); // 【可选】导入KV摘要加速（决策：通过配置开关） if (config_.enable_kv_hint && pending_kv_hint_.has_value()) { llama_cpp_.ImportKVCacheSummary(pending_kv_hint_.value()); pending_kv_hint_.reset(); // 消费后清除 } std::string partial; uint32_t token_count = 0; while (auto token = co_await stream.Next()) { // 【关键】每Token检查取消信号（<1μs开销） if (cancel_token.IsCancelled()) { // 【可选】导出KV摘要用于断点续推 if (config_.enable_kv_hint) { pending_kv_hint_ = llama_cpp_.ExportKVCacheSummary(); } co_return LLMResult{ .partial = partial, .token_count = token_count, .preempted = true, .kv_hint = pending_kv_hint_ // 可选返回 }; } partial += *token; token_count++; // 【可选】每100 Token保存一次摘要（平衡开销与恢复粒度） if (config_.enable_kv_hint && token_count % 100 == 0) { pending_kv_hint_ = llama_cpp_.ExportKVCacheSummary(); } } co_return LLMResult{ .partial = partial, .token_count = token_count, .preempted = false }; } ``` --- ## 六、配置模板（完整示例） ```yaml # config.yaml - AOS-Browser 本地LLM专用版 # ========== 运行时 ========== runtime: cpp_version: "20" thread_pool_size: 4 # 协程调度线程数 max_concurrent_tabs: 3 # ========== LLM配置 ========== llm: model_path: "/models/llama-3-8b-instruct-q4_k_m.gguf" n_threads: 7 # 物理核数-1 n_gpu_layers: 35 # RTX 3060 12GB推荐值 enable_kv_hint: false # 【决策】V1.0默认关闭，V1.1可选启用 kv_hint_max_layers: 8 # 摘要保存的层数（平衡精度与大小） # ========== 浏览器配置 ========== browser: headless: true viewport_width: 1280 viewport_height: 720 navigation_timeout_sec: 30 # ========== DOM哈希配置（关键决策） ========== dom: critical_selectors: - "#main-form" - "input[name=email]" - "input[name=password]" - "#submit-button" - ".product-price" - ".cart-total" - "[data-testid=checkout-btn]" # ========== 安全配置（方案B） ========== security: encrypt_sensitive_fields: true encryption_key_path: "/etc/aos-browser/key.bin" # 权限600，外部注入 sensitive_field_patterns: - ".*password.*" - ".*token.*" - ".*auth.*" - ".*secret.*" - ".*api_key.*" - ".*access_token.*" audit_critical_ops: true audit_log_path: "/var/log/aos-browser/audit.log" allowed_domains: [] # 空表示无限制 # ========== 可观测性配置（方案A） ========== metrics: output_path: "/var/log/aos-browser/metrics.log" max_file_size_mb: 100 max_backup_files: 5 debug_mode: false # 生产环境关闭 # remote_endpoint: "" # 方案B扩展项，默认留空 # ========== 持久化配置 ========== storage: sqlite_path: "/var/lib/aos-browser/state.db" wal_mode: true checkpoint_interval_steps: 5 # 每5步自动保存 atomic_write: true # 启用tmp+fsync+rename # ========== 中断响应配置 ========== interrupt: user_pause_timeout_ms: 100 # 目标：<100ms响应 crash_detection_timeout_ms: 500 emergency_snapshot_enabled: true ``` --- ## 七、下一步行动建议 ✅ **当前阶段完成**：UR/ADR确认 + 顶层架构设计（决策落地） 📋 **下一步：输出 `AOS-Browser-Detailed-Design.md`**，重点定义： 1. **数据结构规范** - `EnhancedBrowserSnapshot` 完整字段定义 + 序列化协议 - `KVCacheSummary` 二进制格式（与定制llama.cpp对齐） - SQLite表结构（含索引/外键/事务策略） 2. **接口边界定义** - `LocalLLMAdapter` 与定制llama.cpp的FFI接口 - `CriticalDOMHasher` 与Playwright的集成方式 - `SecurityManager` 的加密/解密调用时机 3. **构建与部署规范** - 定制llama.cpp分支管理策略（submodule/patch） - 配置密钥的安全注入方式（systemd EnvironmentFile / Docker secret） - 日志轮转与监控脚本示例 4. **测试验收标准** - 崩溃恢复测试用例（含DOM哈希校验场景） - 安全加密验证（敏感字段密文存储 + 非敏感字段明文） - 离线环境部署验证清单 > 请团队确认本架构设计，如无异议，我将基于此输出详细设计文档。